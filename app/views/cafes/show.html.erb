<!--카페 정보 ㄴㄴ-->
<!-- 카페에 등록된 글 ㅇㅇ-->
<h1> <%= @cafe.title %> </h1>
<small><%= @cafe.description%></small>
<hr>

<!--카페 가입버튼 -->
<%= link_to "가입하기", join_cafe_path(@cafe), method: "POST" ,class: "btn btn-warning"%>
<ul class="list-group">
  <% @cafe.posts.each do |post| %>
  
    <li class="list-group-item d-flex justify-content-between align-items-center">
      <%= link_to post.title, post_path(post) %>
      
      <span class="badge badge-primary badge-pill">
        <%=post.comments.count%>
      </span>
    </li>
  <% end %>

</ul>
<%= link_to "글 작성", new_post_path(naver_id: @cafe.id), class: "btn btn-warning" %>
<!-- cafe와 post의 관계를 이용해야함, cafe 하나가 여러 개의 post를 가진다. -->
<!--새글 등록버튼-->